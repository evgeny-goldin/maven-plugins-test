<?xml version="1.0" encoding="UTF-8"?>

<project xmlns              = "http://maven.apache.org/POM/4.0.0"
         xmlns:xsi          = "http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation = "http://maven.apache.org/POM/4.0.0
                               http://maven.apache.org/maven-v4_0_0.xsd">

    <modelVersion>4.0.0</modelVersion>
    <groupId>com.goldin.plugins.maven-plugins-test</groupId>
    <artifactId>main.tests</artifactId>
    <packaging>pom</packaging>
    <version>0.1-SNAPSHOT</version>
    <name>[${project.groupId}:${project.artifactId}:${project.version}]</name>


    <properties>
        <artifactory>http://evgeny-goldin.org/artifactory</artifactory>
        <maven-hudson-plugin-version>0.2-SNAPSHOT</maven-hudson-plugin-version>
        <maven-assert-plugin-version>0.2-SNAPSHOT</maven-assert-plugin-version>
        <maven-copy-plugin-version>0.2-SNAPSHOT</maven-copy-plugin-version>
        <spring-version>3.0.5.RELEASE</spring-version>
        <junit-version>4.8.2</junit-version>
        <log4j-version>1.2.16</log4j-version>
        <jaxen-version>1.1.1</jaxen-version>
        <gmaven-version>1.3</gmaven-version>
        <groovy-version>1.8.0-beta-3</groovy-version>
    </properties>


    <repositories>
        <repository>
            <id>libs-snapshots</id>
            <url>${artifactory}/libs-snapshots</url>
            <releases><enabled>false</enabled></releases>
            <snapshots><enabled>true</enabled></snapshots>
        </repository>
        <repository>
            <id>libs-releases</id>
            <url>${artifactory}/libs-releases</url>
            <releases><enabled>true</enabled></releases>
            <snapshots><enabled>false</enabled></snapshots>
        </repository>
    </repositories>


    <pluginRepositories>
        <pluginRepository>
            <id>plugins-snapshots</id>
            <url>${artifactory}/plugins-snapshots</url>
            <releases><enabled>false</enabled></releases>
            <snapshots><enabled>true</enabled></snapshots>
        </pluginRepository>
        <pluginRepository>
            <id>plugins-releases</id>
            <url>${artifactory}/plugins-releases</url>
            <releases><enabled>true</enabled></releases>
            <snapshots><enabled>false</enabled></snapshots>
        </pluginRepository>
    </pluginRepositories>


    <distributionManagement>
        <repository>
            <id>plugins-releases-local</id>
            <name>plugins-releases-local</name>
            <url>${artifactory}/plugins-releases-local</url>
        </repository>
    </distributionManagement>


    <build>

        <pluginManagement>
            <plugins>

                <!-- ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ -->
                <!-- Configuring Jar plugin to pack anything -->
                <!-- ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ -->

                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-jar-plugin</artifactId>
                    <version>2.3.1</version>
                    <configuration>
                        <includes>
                            <include>nothing</include>
                        </includes>
                    </configuration>
                </plugin>

                <!-- ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ -->
                <!-- Configuring GMaven plugin dependencies -->
                <!-- ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ -->

                <plugin>
                    <groupId>org.codehaus.gmaven</groupId>
                    <artifactId>gmaven-plugin</artifactId>
                    <version>${gmaven-version}</version>
                    <dependencies>
                        <dependency>
                            <groupId>org.codehaus.gmaven.runtime</groupId>
                            <artifactId>gmaven-runtime-1.7</artifactId>
                            <version>${gmaven-version}</version>
                            <exclusions>
                                <exclusion>
                                    <groupId>org.codehaus.groovy</groupId>
                                    <artifactId>groovy-all</artifactId>
                                </exclusion>
                            </exclusions>
                        </dependency>
                        <dependency>
                            <groupId>org.codehaus.groovy</groupId>
                            <artifactId>groovy-all</artifactId>
                            <version>${groovy-version}</version>
                        </dependency>
                    </dependencies>
                </plugin>
            </plugins>
        </pluginManagement>

        <plugins>

            <!-- ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ -->
            <!-- Enforcing Maven/Java versions -->
            <!-- ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ -->

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-enforcer-plugin</artifactId>
                <version>1.0</version>
                <inherited>false</inherited>
                <executions>
                    <execution>
                        <id>maven-enforcer-display-info</id>
                        <goals><goal>display-info</goal></goals>
                        <phase>validate</phase>
                    </execution>
                    <execution>
                        <id>maven-enforcer-enforce</id>
                        <goals><goal>enforce</goal></goals>
                        <phase>validate</phase>
                    </execution>
                </executions>
                <configuration>
                    <rules>
                        <requireMavenVersion>
                            <version>2.2.1</version>
                        </requireMavenVersion>
                        <requireJavaVersion>
                            <version>1.6</version>
                        </requireJavaVersion>
                    </rules>
                </configuration>
            </plugin>

            <!-- ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ -->
            <!-- Displaying plugin versions checked -->
            <!-- ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ -->

            <plugin>
                <groupId>org.codehaus.gmaven</groupId>
                <artifactId>gmaven-plugin</artifactId>
                <version>${gmaven-version}</version>
                <inherited>false</inherited>
                <executions>
                    <execution>
                        <id>plugins-versions</id>
                        <goals>
                            <goal>execute</goal>
                        </goals>
                        <phase>initialize</phase>
                        <configuration>
                            <source>
                                def date = new Date()
                                println """
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
Groovy [$GroovySystem.version], [$date] - testing plugin versions:
* maven-copy-plugin-version   : "${maven-copy-plugin-version}"
* maven-hudson-plugin-version : "${maven-hudson-plugin-version}"
* maven-assert-plugin-version : "${maven-assert-plugin-version}"
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~"""
                            </source>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>


    <modules>
        <module>maven-hudson-plugin</module>
        <module>maven-copy-plugin</module>
    </modules>


    <profiles>
        <profile>
            <id>TeamCity</id>
            <activation>
                <property>
                    <name>maven.repo.local</name>
                </property>
            </activation>
            <!-- ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ -->
            <!-- Cleaning up local Maven repo to re-download all dependencies -->
            <!-- ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ -->
            <build>
                <plugins>
                    <plugin>
                        <groupId>com.goldin.plugins</groupId>
                        <artifactId>maven-copy-plugin</artifactId>
                        <version>${maven-copy-plugin-version}</version>
                        <inherited>false</inherited>
                        <executions>
                            <execution>
                                <id>delete-plugins</id>
                                <goals>
                                    <goal>copy</goal>
                                </goals>
                                <phase>initialize</phase>
                                <configuration>
                                    <resource>
                                        <directory>${maven.repo.local}</directory>
                                        <excludes>
                                            <!-- Maven 2 starts failing otherwise -->
                                            <exclude>org/apache/**</exclude>
                                            <exclude>org/codehaus/**</exclude>
                                        </excludes>
                                        <clean>true</clean>
                                        <verbose>true</verbose>
                                    </resource>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

</project>
