<?xml version="1.0" encoding="UTF-8"?>

<project xmlns              = "http://maven.apache.org/POM/4.0.0"
         xmlns:xsi          = "http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation = "http://maven.apache.org/POM/4.0.0
                               http://maven.apache.org/maven-v4_0_0.xsd">

    <modelVersion>4.0.0</modelVersion>
    <groupId>com.goldin.plugins</groupId>
    <artifactId>maven-plugins-test</artifactId>
    <packaging>pom</packaging>
    <version>0.1-SNAPSHOT</version>
    <name>[${project.groupId}:${project.artifactId}:${project.version}]</name>


    <properties>
        <artifactory>http://evgeny-goldin.org/artifactory</artifactory>
        <maven-hudson-plugin-version>0.1-SNAPSHOT</maven-hudson-plugin-version>
        <maven-assert-plugin-version>0.1-SNAPSHOT</maven-assert-plugin-version>
    </properties>


    <repositories>
        <repository>
            <id>remote-repos</id>
            <url>${artifactory}/remote-repos</url>
            <releases><enabled>true</enabled></releases>
            <snapshots><enabled>false</enabled></snapshots>
        </repository>
    </repositories>


    <pluginRepositories>
        <pluginRepository>
            <id>plugins-snapshots</id>
            <url>${artifactory}/plugins-snapshots</url>
            <releases><enabled>false</enabled></releases>
            <snapshots><enabled>true</enabled></snapshots>
        </pluginRepository>
        <pluginRepository>
            <id>plugins-releases</id>
            <url>${artifactory}/plugins-releases</url>
            <releases><enabled>true</enabled></releases>
            <snapshots><enabled>false</enabled></snapshots>
        </pluginRepository>
    </pluginRepositories>


    <distributionManagement>
        <repository>
            <id>plugins-releases-local</id>
            <name>plugins-releases-local</name>
            <url>${artifactory}/plugins-releases-local</url>
        </repository>
    </distributionManagement>


    <!-- ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ -->
    <!-- Displaying plugin versions checked -->
    <!-- ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ -->

    <build>
        <plugins>
            <plugin>
                <groupId>org.codehaus.gmaven</groupId>
                <artifactId>gmaven-plugin</artifactId>
                <version>1.3</version>
                <inherited>false</inherited>
                <executions>
                    <execution>
                        <id>plugins-versions</id>
                        <goals>
                            <goal>execute</goal>
                        </goals>
                        <phase>initialize</phase>
                        <configuration>
                            <source>
                                def date = new Date()
                                println """
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
[$date] - testing plugin versions:
* maven-hudson-plugin-version : "${maven-hudson-plugin-version}"
* maven-assert-plugin-version : "${maven-assert-plugin-version}"
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
"""
                            </source>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <modules>
        <module>maven-hudson-plugin</module>
    </modules>

</project>
